{
  "name": "whatsapp-bot",
  "version": "1.0.0",
  "description": "WhatsApp bot for sending emails",
  "main": "bot.js",
  "scripts": {
    "start": "node bot.js",
    "postinstall": "node -e \"try { require('fs').chmodSync('startup.sh', '755') } catch (e) { console.log('Skipping chmod on Windows') }\""
  },
  "dependencies": {
    "express": "^4.18.2",
    "qrcode-terminal": "^0.12.0",
    "@sendgrid/mail": "^8.1.0",
    "whatsapp-web.js": "^1.23.0"
  },
  "engines": {
    "node": ">=20.0.0"
  }
}
